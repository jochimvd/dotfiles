#!/usr/bin/env bash

# Clipboard manager using walker

selection=$(cliphist list | walker --dmenu -p Clipboard --label 2)

# exit if walker was cancelled
if [ "$selection" = "CNCLD" ] || [ -z "$selection" ]; then
    exit 0
fi

echo -n "$selection" | cliphist decode | wl-copy